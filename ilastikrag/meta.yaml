package:
    name: ilastikrag

  {% if GIT_DESCRIBE_NUMBER|int == 0 %}
    version: {{GIT_DESCRIBE_TAG}}
  {% else %}
    # If we're using a non-tagged revision, append '.postN' to the version
    version: {{GIT_DESCRIBE_TAG}}.post{{GIT_DESCRIBE_NUMBER}}
  {% endif %}

source:
    git_url: https://github.com/stuarteberg/ilastikrag
    # git_tag: 34ca57aec4df481a39feda7414e11c307045d84e # master 2016/07/07
    git_tag: 0b83d53b6217f58b8c2fdbe8aa815bd37c7e536b # master 2017/04/26
    patches:
      - python3_print.patch

build:
  number: 0

requirements:
  build:
    - python 3.5*
    - setuptools

  run:
    - python 3.5*
    - numpy  >=1.9
    # - hdf5   >=1.8
    - h5py   >=2.6
    - pandas >=0.16
    - vigra  >=1.11
    - networkx >=1.11

test:
  requires:
    - nose  >=1.3
    # - hdf5   >=1.8

  imports:
    - ilastikrag

about:
  home: https://github.com/stuarteberg/ilastikrag
  license: Simplified BSD
  license_file: LICENSE-BSD
  summary: ND Region Adjacency Graph with edge feature algorithms
